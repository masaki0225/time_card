<div class="mobile-title">
  <h1 class="mobile-t"><%= link_to "Project To Do", root_path %></h1>
  <p class="mobile-command">
    <% if logged_in? %>
      <%= link_to "New Project", projects_new_path %>
      <%= link_to @current_user.name, user_path(current_user) %>
    <% else %>
      <%= link_to "Sign Up", signup_path %>
      <%= link_to "Login", login_path %>
    <% end %>
  </p>
</div>  
<div class="title">
  <%= image_tag 'flowy.jpg', class: 'flowy' %>
  <div class="selif-left">
    <% if logged_in? %>
      <p><%= current_user.name %>,新しいプロジェクトを作成するの？</p>
      <p>なんて素敵なアイデア！</p>
      <p><%= link_to "New Project", projects_new_path %></p>
    <% else %>
      <p>やぁ！</p>
      <p>初めての方は<%= link_to "Sign Up", signup_path %>をしてね</p>
      <p>参加メンバーは<%= link_to "Login", login_path %>をしてね</p>
    <% end %>
  </div>
</div>
<div class="undertale">
  <%= render "layouts/nav_home" %>
</div>
